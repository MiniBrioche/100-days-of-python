# Range is a complex type and is iterable (can be used in loops)
print(type(range(5)))

# Iterable:

# Ranges
for x in range(5):
    print(x)

# Strings
for x in "Python":  # Each letter get's it's own "turn"/iteration
    print(x)
